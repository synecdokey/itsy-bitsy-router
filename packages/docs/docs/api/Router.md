# \<Router />

The react element provided by [`useRoutes`](./Hooks.md#useRoutes).

> Note: This is likely to change before release.

## render: ({children}) => ReactElement

Enables a surrounding layout to be created, where `children` is the element
rendered by the router.
